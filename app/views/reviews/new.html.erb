<h1>Add a new review</h1>
<div class="container">
  <%= simple_form_for [@spot, @review] do |f| %>
    <%= f.input :comment %>
    <%= f.submit "Submit", class:'btn btn-primary w-100' %>
  <% end %>
</div>
