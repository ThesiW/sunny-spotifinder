
<p><%= @spot.address %></p>
<p><%= @spot.pictures %></p>
<p><%= @spot.sun_start %></p>
<p><%= @spot.sun_end%></p>
<!--  <div>
  <%# if user_signed_in? %>
   <%#= cl_image_tag current_user.user_picture, class:"avatar-bordered" %>
  <%#end %>
 </div>
 -->
<%= link_to "Back to spots", spots_path  %><br>
<%= link_to "Back to randomise", randomise_spots_path  %><br>
<%= link_to "Back to Home", root_path  %>

<%= simple_form_for [@spot, @bookmark] do |b| %>
  <%= b.submit class: 'btn btn-primary' %>
<% end %>

<%= simple_form_for [@spot, @favourite] do |f| %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>



<% @spot.bookmarks.each do |b| %>
  <% if b == Bookmark.find_by(spot: @spot, user: current_user)%>
    <%= button_to "delete", bookmark_path(b), :method => :delete %>
  <% else %>
    <%= simple_form_for [@spot, @bookmark] do |b| %>
      <%= b.submit class: 'btn btn-primary' %>
    <% end %>
  <% end %>
<% end %>

